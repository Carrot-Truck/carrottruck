import styled from 'styled-components';

export const OrderListItemWrapper = styled.div`
  // font-family: NanumSquareNeo;
  font-size: 1rem;
  padding: 10px 0px;
  text-align: left;
  border-bottom: 1px solid var(--black-100);
  color: var(--gray-400);

  p {
    padding: 3px 10px;
  }
`;
